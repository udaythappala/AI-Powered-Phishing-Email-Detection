{
  "name": "phishguard-soc-frontend",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev -- --host 0.0.0.0",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview -- --host 0.0.0.0"
  },
  "dependencies": {
    "axios": "^1.20.0"
  }
}
